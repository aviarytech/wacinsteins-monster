{
  "scripts": {
    "cy:headless": "cypress run --browser=chrome",
    "cy:debug": "DEBUG=cypress:cli cypress run",
    "cy:headed": "npx cypress run --headed --no-exit",
    "cy:launcher": "cypress:launcher"
  },
  "devDependencies": {
    "@testing-library/cypress": "^8.0.1",
    "cypress": "^8.4.0"
  }
}
